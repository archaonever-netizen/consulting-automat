"""Помощник по исправлению данных (Фаза 7).

Срабатывает ТОЛЬКО на структурных проблемах данных (нехватка/противоречие/
невыполнимость), а не на промахах модели (их лечит ретрай/верификатор).
Помогает человеку по-человечески, предлагает рычаги, считая недостающее из
данных, но НЕ выдумывая; принятая правка становится user_input и заново проходит
декомпозицию и верификатор.

Слои:
- types      — типы проблем и рычагов (чистые dataclass-и).
- classifier — детерминированная классификация: данные vs модель (шаг 1).
- assistant  — ход диалога с лёгкой моделью (шаг 2, позже).
"""
