"""Фреймворк декомпозиции целей: месяцы → недели → дни.

Пользователь задаёт измеримую цель, ИИ предлагает разбиение по уровням, каждый
уровень согласует человек. ИИ оперирует только проверяемыми данными; всё
недостающее — явный DataGap или Assumption, а не выдуманное число.

Источники истины — спецификация в decompsition/goal-decomposition-framework.md
и системный промпт движка в decompsition/claude-system-prompt.md.

Слои пакета:
- domain        — каноническая модель документа (Pydantic v2), инварианты.
- verifier      — детерминированная анти-обман проверка предложения (фаза 2).
- llm / engine  — вызов модели и сборка предложения (фаза 3).
- state_machine — конечный автомат согласования узла (фаза 4).
- recalc        — каскадный пересчёт с сохранением ручных правок (фаза 4).
"""
