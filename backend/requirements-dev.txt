# Зависимости для разработки и тестов. В прод-образ НЕ попадают:
# Dockerfile устанавливает только requirements.txt.
# Установка локально:  pip install -r backend/requirements-dev.txt
-r requirements.txt

pytest>=8.0
pytest-asyncio>=0.24
ruff
mypy

# Нужен только при DECOMPOSITION_LLM_PROVIDER=anthropic (в коде ленивый импорт,
# по умолчанию движок ходит через Promptra и этот пакет не требуется).
langchain-anthropic>=0.2.0
