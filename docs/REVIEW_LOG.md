# Журнал ревью и прокачки ШЕФ (consulting-automat)

Этот файл — рабочая память ревью: план, находки, изменения и их причины.
Обновляется после каждого этапа. Дата начала: 12.06.2026.

## 🔴 СРОЧНО

Критичных проблем безопасности, требующих немедленных действий, **не обнаружено**.

Проверено отдельно: файл с секретами `.env.local` в git **не попал** (отслеживаются
только безопасные `.env.example` и фронтовые конфиги без секретов).

Найдены две некритичные уязвимости, исправляются в Этапе 4/6:
1. **Path traversal** в раздаче файлов SPA (`backend/main.py`, serve_spa) — злоумышленник
   мог бы запросом вида `/../backend/...` прочитать файлы исходников на сервере.
2. **Нет ограничения частоты запросов** к ИИ-эндпоинтам (чат, декомпозиция) — авторизованный
   пользователь может случайно/намеренно сжечь бюджет LLM-API частыми запросами.

---

## Карта проекта (Этап 0 — разведка)

**Что это:** веб-приложение для консалтинга «ШЕФ»: клиенты и брифы, структура компании
(функции/отделы), цели с ИИ-декомпозицией, задачи, ИИ-чат, оркестрация сети агентов,
трекер Kaiten, база знаний (BPMM).

**Стек:**
- Бэкенд: Python 3.12, FastAPI, async SQLAlchemy 2.0, LangChain/LangGraph (ИИ-агенты).
- Фронтенд: React 19 + TypeScript, Vite 8 (на Rolldown), react-router 7, axios.
- БД: SQLite (`instance/app.db` локально; на проде — файл на смонтированном диске `/data/app.db`).
- Деплой: Amvera, Docker. **Важно:** собранный фронтенд `frontend/dist` коммитится в репозиторий
  и попадает в Docker-образ как есть. Перед коммитом dist обязан собираться с пустым
  `VITE_API_URL` (относительные адреса API), иначе в прод зашьётся localhost.

**Структура папок:**
| Папка | Назначение |
|---|---|
| `backend/` | FastAPI: `main.py` (вход), `models.py` (22 таблицы), `routes/` (API), `services/` (логика), `core/` (конфиг, БД, шифрование) |
| `backend/services/agent_network/` | сеть ИИ-агентов на LangGraph (см. ARCHITECTURE_AGENTS.md) |
| `backend/services/goal_decomposition/` | фреймворк декомпозиции целей + верификатор + тесты |
| `frontend/src/` | React: `pages/` (16 экранов), `components/` (Layout, виджеты), `services/api.ts` (axios+JWT) |
| `frontend/dist/` | прод-сборка (коммитится!) |
| `BPMM/` | источник базы знаний (PDF + текст + фрагменты для RAG) |
| `decompsition/` | документация фреймворка декомпозиции (промпты, спека) |
| `instance/` | локальная SQLite-БД (в git не попадает) |
| `docs/` | документация (этот журнал, заметки, архитектура) |
| `templates/`, `static/` | ⚰️ мёртвое наследие старого Flask-приложения (удаляется в Этапе 1) |

**Точки входа:**
- Бэкенд: `python -m uvicorn backend.main:app --port 8000` (старт ~9 сек: создание таблиц, сид основателя и базы знаний).
- Фронтенд дев: `npm run dev` в `frontend/` (порт 5173); прод: FastAPI раздаёт `frontend/dist`.
- Авторизация: JWT, логин по email/паролю; основатель сидится из `FOUNDER_EMAIL`/`FOUNDER_PASSWORD`.

**Хранение данных:** всё в одной БД (22 таблицы): пользователи, клиенты, брифы+секции,
функции/отделы и связи, задачи, чат-сессии/сообщения, цели и декомпозиция, база знаний
(категории/статьи/источники/фрагменты), подключение Kaiten (токен шифруется Fernet-ключом,
производным от SECRET_KEY). На фронте в localStorage — только JWT-токен и userId.

---

## План этапов и статус

| Этап | Содержание | Статус |
|---|---|---|
| 0 | Подготовка: коммит WIP, журнал, baseline-проверки | ✅ готово |
| 1 | Уборка мёртвого кода (Flask-наследие) | ✅ готово |
| 2 | Консолидация заметок → docs/NOTES.md + корневой README | ✅ готово |
| 3 | docs/ARCHITECTURE.md | ✅ готово |
| 4 | Производительность: path traversal fix, кэш-заголовки, code splitting, react-query | ✅ готово |
| 5 | Поиск багов по сценариям | ✅ готово |
| 6 | Безопасность: rate limiting, прод-инструкции | ✅ готово |

Правила: один логический блок = один коммит; после каждого шага — проверки
(pytest; tsc+vitest+build; grep dist на localhost:8000); откат любого шага — `git revert <sha>`.
Никаких push — деплой на Amvera владелец делает сам.

---

## Журнал

### Этап 0 — Подготовка (12.06.2026) ✅

**Сделано:**
1. Незавершённые правки прошлой сессии (классификатор `data_fix` декомпозиции целей)
   закоммичены как есть отдельным коммитом `d5141f0` — точка отсчёта.
2. Создан этот журнал.
3. В `.gitignore` добавлены `*.docx` и временные логи.

**Baseline (всё зелёное до начала изменений):**
- Тесты бэкенда: **90 passed** (pytest, 4.3 с).
- Типы фронтенда: tsc OK; тесты фронтенда: **13 passed** (vitest).
- Прод-сборка: воспроизводится байт-в-байт; единый бандл
  `index-BoJ7np8Y.js` = **560 582 байта** (gzip 164.77 KB) + CSS 73 327 байт. Code splitting нет.
- Сервер: стартует за ~9 с, `/api/health` → 200, SPA → 200.

**Примечание владельцу:** в корне проекта лежит личный документ
`Договор_оказания_услуг_РК_ИП_В2В канал продаж Краснодар (1).docx`. Я его не трогаю
(в git он теперь гарантированно не попадёт благодаря `*.docx` в .gitignore), но рекомендую
вручную перенести его из папки проекта в личные документы.

---

### Этап 1 — Уборка мёртвого кода (12.06.2026) ✅

Перед удалением каждый пункт проверен: grep по живому коду (`backend/`, `frontend/src`,
`frontend/dist`), Dockerfile (копирует только backend, frontend/dist, BPMM), amvera.yaml, доки.

**Удалено (уверенность 100%, всё восстановимо из git-истории):**
| Что | Почему мёртвое |
|---|---|
| `templates/` (18 Jinja-шаблонов) | UI старого Flask; FastAPI раздаёт React из frontend/dist, ссылок нет |
| `static/` (css, js, img) | стили уже портированы в `frontend/src/styles/styles.css`; на `/static` не ссылается ни бэкенд, ни собранный фронт |
| `agents.py`, `prompts.py` | импортируют `function_agent`, `orchestrator`, `models` — этих модулей в корне больше нет; новая сеть агентов живёт в `backend/services/agent_network/` |
| `debug_home.py`, `debug_login.py`, `debug_route.py`, `test_app.py` | импортируют несуществующий модуль `app` (старый Flask) — даже не запускаются |
| `create_example.py`, `check_users.py`, `seed_founder.py` | тоже `from app import …` — сломаны; сид основателя теперь автоматический при старте бэкенда (коммит b228834) |
| `test_screenshot.py` | одноразовый скриншот-скрипт Flask-эпохи |
| `Procfile` | команда `gunicorn app:app` для несуществующего Flask-приложения |

**Сознательно оставлено:**
- `create_test_user.py` — рабочий инструмент, упоминается в QUICKSTART/START_HERE/start.ps1.
- `start.ps1` — хелпер локального запуска (актуален для FastAPI).
- `requirements.txt` (корень) — актуальный прокси на `backend/requirements.txt`.

### Этап 2 — Консолидация заметок и документации (12.06.2026) ✅

**Создано:**
- `docs/NOTES.md` — все заметки/идеи/бэклог в одном месте: нереализованные улучшения
  модуля задач, факт незавершённого переноса Яндекс.Календаря, выжимка дизайн-системы
  (токены, принципы), хронология проекта, карта живой документации.
- `README.md` (корневой, раньше его не было) — как запустить, логин, troubleshooting,
  правило деплоя (пересборка dist с пустым VITE_API_URL), оглавление доков.
  Объединил QUICKSTART.md + START_HERE.md.

**Перемещено в `docs/archive/`** (пригодится, если возрождать Яндекс.Календарь):
- TASKS_SETUP.md, YANDEX_CALENDAR_SETUP.md (инструкции Flask-эпохи).

**Удалено после переноса ценного в NOTES.md** (полные тексты — в git-истории):
- статусные отчёты от 09.06.2026: CHECKLIST.md, DONE.md, PLAN_COMPLETION.md,
  IMPLEMENTATION_SUMMARY.md;
- QUICKSTART.md, QUICK_START.md (Flask-эпоха), START_HERE.md — заменены README.md;
- `design_handoff_shef/` (22 файла) — хендофф дизайна под СТАРЫЙ Flask; дизайн давно
  портирован в `frontend/src/styles/styles.css`, ключевые токены сохранены в NOTES.md §3.

### Этап 3 — Техническая документация (12.06.2026) ✅

Создан `docs/ARCHITECTURE.md`: общая схема (браузер → React → FastAPI → БД/ИИ),
структура папок, все 16 экранов и навигация, устройство хранения данных,
ИИ-слой (LLM через Promptra, сеть агентов, декомпозиция, база знаний),
окружение/деплой и история значимых изменений по git. Написан понятным языком
для владельца-непрограммиста.

### Этап 4 — Производительность и UX (12.06.2026) ✅

Цель: «все переходы тормозят» (со слов владельца). Причины: один JS-файл 560 КБ
без кэширования + полный перезапрос данных на каждый переход. Сделано 9 шагов,
каждый — отдельный коммит с проверками (tsc, vitest, build, grep dist, смоук uvicorn):

| Шаг | Коммит | До → После |
|---|---|---|
| Path traversal fix (безопасность, см. Этап 6) | 8747c5c | дыра → атаки получают index.html |
| Cache-Control на статику | 5e1808b | браузер качал бандл каждый визит → /assets/* кэш на год (immutable), index.html — 304 |
| Vendor-чанк (react/router/axios/react-query) | 95b8cc6, c7779c1 | один файл → стабильный vendor 298 КБ в вечном кэше |
| React.lazy: 3 ИИ-экрана с markdown | d55f504 | react-markdown 115 КБ грузится только при заходе в Чат/Знания/Оркестрацию |
| React.lazy: остальные 11 экранов | 0ece2bc | **стартовый JS 560 → 319 КБ**, экраны по 1–22 КБ по требованию |
| react-query: Tasks | 474053a | повторный вход — мгновенно из кэша (30 с), потом данные сразу + фоновое обновление |
| react-query: Goals, Home | 35695e7 | то же; профиль (ключ `me`) грузится 1 раз на 5 мин |
| react-query: Clients, ClientDetail | 659ecd9 | мутации инвалидируют список+карточку+сводку разом |
| react-query: Company, Knowledge | dc430ca | то же для матрицы и базы знаний |
| Layout через useQuery + clear() на выходе | 2c66081 | белый экран при каждом hard reload → мгновенный каркас |

**Не трогал сознательно:** ChatPage (стриминг SSE), TrackerPage (уже свой кэш с
коммита ff8eac3), GoalDecompositionPage (сложная state-машина) — работают как раньше.

**Поведение для пользователя:** переход между разделами теперь не перезапрашивает
данные (до 30 секунд — мгновенно из кэша; после — старые данные сразу на экране,
обновление в фоне). Код экрана качается один раз и навсегда оседает в кэше браузера.
Hard reload не показывает белый экран. Навигация при докачке кода держит текущую
страницу на экране (React Router оборачивает переходы в startTransition).

### Этап 5 — Поиск багов (12.06.2026) ✅

Найдено и исправлено:
1. **Сообщение «неверный пароль» никогда не показывалось** (4c63614). Перехватчик 401
   перезагружал страницу на /login для любого 401 — включая ответ самого логина.
   Теперь ошибка логина отдаётся форме, а 401 на других запросах по-прежнему разлогинивает.
2. **Устаревшие данные после сохранения** (4c63614) — побочный эффект нового кэша:
   страницы, меняющие данные напрямую (бриф, функция/отдел, декомпозиция), не сбрасывали
   кэш. Решено системно: `api.ts` сам сбрасывает связанные ключи после любой успешной
   мутации (карта правил по URL) — покрывает и будущие страницы.
3. **Вечная «Загрузка…» на главной при недоступном бэкенде** (0ed7cb6) — теперь
   показывается понятное сообщение об ошибке.
4. **create_test_user.py не запускался** (65305b6) — неверный импорт SQLAlchemy
   и неучтённый Postgres-URL; переписан на общий engine приложения.

Проверено и в порядке: каскадное удаление клиента (брифы/чаты/задачи удаляются вместе),
пустые состояния всех списков, защита всех роутов авторизацией, обработка ошибок форм
логина/целей/клиентов.

**Известные недоделки (НЕ баги ревью, остались от миграции — кандидаты в бэклог):**
- Кнопки «Создать задачу», «Редактировать», «Завершить» на экране Задач — без действий
  (UI есть, обработчиков нет; функционал задач из Flask-версии перенесён частично).
- Интеграция Яндекс.Календаря не перенесена (см. NOTES.md §2).

### Этап 6 — Безопасность (12.06.2026) ✅

- **Path traversal закрыт** ранее (8747c5c, проверено живыми атаками).
- **Лимит частоты ИИ-запросов** (65305b6): чат — 20/мин на пользователя,
  LLM-операции целей — 10/мин суммарно; 429 с понятным текстом; 3 авто-теста.
- Подтверждено: секретов в git нет; все роуты под JWT; ввод валидируется Pydantic;
  SQL — только через ORM (инъекции исключены); Kaiten-токен шифруется (Fernet от
  SECRET_KEY); личные данные (задачи/чат/цели/Kaiten) фильтруются по пользователю.
- Осознанное решение зафиксировано: клиенты/брифы/компания/база знаний — общие для
  всех авторизованных сотрудников (приложение одной компании).

**🟡 Чек-лист для прода (Amvera, env-переменные) — проверить владельцу:**
1. `SECRET_KEY` задан и НЕ равен дефолтному `shef-dev-secret-key-change-in-prod`.
   ⚠️ Смена SECRET_KEY разлогинит всех и сделает нечитаемыми сохранённые Kaiten-токены
   (их придётся переподключить в профиле).
2. `FRONTEND_URL` = реальный домен приложения (для CORS; впрочем, фронт и API на одном
   домене, так что CORS-запросов в норме нет).
3. `FOUNDER_PASSWORD` — стойкий пароль (он переустанавливается при каждом старте из env).

**🟡 РИСК для владельца — дев смотрит в облачную базу:** `DATABASE_URL` в локальном
`.env.local` указывает на облачный Postgres (Supabase). Это значит: локальные
эксперименты работают с ЖИВЫМИ данными (тест-пользователь чуть не создался там — 
остановлено). Рекомендация: для локальной разработки убрать/закомментировать
DATABASE_URL в .env.local — приложение само перейдёт на локальный SQLite
(instance/app.db). Я конфиг не менял (это ваше решение).

---

## ИТОГОВОЕ SUMMARY (12.06.2026)

**Сделано за ревью (19 коммитов, всё откатывается по одному через `git revert`):**
- 🧹 Удалено 6 400+ строк мёртвого Flask-наследия и 5 200 строк устаревших доков
  (31 + 33 файла); всё в git-истории.
- 📚 Документация: новый README.md (вход), docs/ARCHITECTURE.md (устройство понятным
  языком), docs/NOTES.md (все заметки/идеи/бэклог в одном месте), этот журнал.
- ⚡ Скорость: стартовый JS **560 → 319 КБ**, экраны грузятся по требованию (1–22 КБ)
  и оседают в годовом кэше браузера; данные кэшируются между экранами — повторные
  переходы мгновенные, обновления в фоне; hard reload без белого экрана.
- 🐛 4 бага исправлено (невидимая ошибка логина — главный).
- 🔐 Безопасность: path traversal закрыт, лимиты на платные ИИ-запросы, прод-чеклист.
- ✅ Тесты: 90 → 93 (backend) + 13 (frontend), все зелёные; сборка воспроизводима,
  dist пересобран с относительными URL (правило localhost соблюдено).

**Что осталось / на что обратить внимание:**
1. 🟡 Прод-чеклист и риск дев-на-прод-БД — выше, требуют ваших действий.
2. Кандидат на удаление: `yandex_calendar.py` (ниже) — ждёт вашего решения.
3. Бэклог идей — docs/NOTES.md §1; недоделки экрана Задач — Этап 5 выше.
4. После деплоя стоит проверить прод глазами: вход, переходы, чат, декомпозиция.
5. Скелетоны загрузки и оптимистичные обновления — сознательно отложены: кэш уже
   убрал 95% ожиданий; добавлять стоит точечно, если что-то останется заметным.

---

## Кандидаты на удаление, требующие подтверждения владельца

1. **`yandex_calendar.py`** (корень) — интеграция с Яндекс.Календарём (CalDAV + OAuth).
   Сейчас НЕ используется кодом, но: в БД сохранены поля токенов (`backend/models.py:36-39`),
   в конфиге — настройки «kept for future» (`backend/core/config.py:55-58`), и на него
   ссылаются доки TASKS_SETUP / YANDEX_CALENDAR_SETUP. Если интеграция календаря в планах —
   оставить; если нет — можно удалить вместе с полями и доками. **Пока оставлен.**

## Что осталось / на что обратить внимание

См. «ИТОГОВОЕ SUMMARY» выше — там сводный список с приоритетами.

---

# Пост-ревью: доработки (12.06.2026, ветка post-review-fixes)

Решения владельца по итогам ревью: дев отвязать от облачной БД; yandex_calendar.py
сохранить (интеграция в планах); кнопки экрана «Задачи» сделать рабочими.

### Задача 1 — Локальная разработка отвязана от боевой базы ✅

- В `.env.local` строка `DATABASE_URL` (Supabase) закомментирована с пояснением —
  приложение локально работает на SQLite `instance/app.db` (дефолт из config.py).
  Файл в git не отслеживается, поэтому правка только на этой машине.
- Проверено: сервер поднимается на SQLite без ошибок, `/api/health` → 200.
  Бонус: старт ускорился ~9 с → ~2 с (не ходим в eu-west-1 при запуске).
- Сиды/миграции не понадобились: таблицы создаются на старте автоматически
  (create_all + идемпотентные ALTER), база знаний сидится сама при пустой БД.
- Нюанс: в локальной базе существовал test@example.com с паролем времён Flask
  (несовместимый формат хэша) — пароль сброшен на `password123` одноразовым
  скриптом (скрипт удалён). Логин проверен — работает.

### Задача 2 — yandex_calendar.py сохранён (решение владельца) ✅

- Файл больше НЕ кандидат на удаление: интеграция с Яндекс.Календарём в планах.
- В докстринг добавлен TODO-маркер с планом переноса (async, подключение к
  tasks-сервису, шифрование токенов как у Kaiten).
- В docs/NOTES.md §1 добавлен пункт в отложенные задачи, чтобы не потерялся.
