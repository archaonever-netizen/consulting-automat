{% extends "base.html" %}
{% block title %}{{ client.name }} — Анкеты{% endblock %}
{% block content %}
<h2 class="mb-4">📋 {{ client.name }}</h2>
<p class="text-muted mb-4">Выберите анкету для заполнения или просмотра.</p>
<div class="row row-cols-1 row-cols-md-3 g-4">
    {% for brief in briefs %}
    <div class="col">
        <div class="card h-100">
            <div class="card-body d-flex flex-column">
                <h5 class="card-title">{{ brief.name }}</h5>
                <p class="card-text small text-muted flex-grow-1">{{ brief.desc }}</p>
                <div class="d-flex justify-content-between align-items-center mt-3">
                    <span class="badge 
                        {% if brief.status == 'Заполнено' %} bg-success 
                        {% elif brief.status == 'В работе' %} bg-warning text-dark 
                        {% else %} bg-secondary {% endif %}">
                        {{ brief.status }}
                    </span>
                    <a href="{{ url_for('brief_form', brief_id=brief.id) }}" class="btn btn-sm btn-outline-primary">Открыть</a>
                </div>
            </div>
            <div class="card-footer bg-transparent border-top-0">
                <small class="text-muted">Изменено: {{ brief.updated_at or 'ещё нет' }}</small>
            </div>
        </div>
    </div>
    {% endfor %}
</div>
<a href="{{ url_for('index') }}" class="btn btn-link mt-4">← Назад к списку клиентов</a>
{% endblock %}