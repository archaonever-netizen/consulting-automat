# Загружается ТОЛЬКО при сборке (npm run build, production mode).
# Пустой VITE_API_URL = относительные URL на тот же домен (нужно для прода Amvera).
# НЕ ставить сюда localhost — иначе прод будет стучаться в localhost.
VITE_API_URL=
